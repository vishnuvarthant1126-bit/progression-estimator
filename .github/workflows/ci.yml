name: ci

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - name: Install
        run: pip install -r requirements.lock && pip install -e . --no-deps
      - name: Unit, parity and artifact tests
        run: pytest -q
      - name: Registry reproduces from code
        run: python -m progression.train --check
